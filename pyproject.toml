[project]
name = "carro-autonomo"
version = "0.1.0"
requires-python = ">=3.10, <3.11"
dependencies = [
    "fake-rpi ; sys_platform != 'linux'",
    "numpy",
    "opencv-python",
    "RPi.GPIO ; sys_platform == 'linux'",
]
