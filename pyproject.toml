[project]
name = "carro-autonomo"
version = "0.1.0"
requires-python = "==3.10"
dependencies = [
    "fake-rpi==0.7.1 ; sys_platform != 'linux'",
    "numpy==2.4.6",
    "opencv-python>=5.0.0.93",
    "RPi.GPIO==0.7.1 ; sys_platform == 'linux'",
]
